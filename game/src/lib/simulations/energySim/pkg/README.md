# energy-sim-wasm (vendored)

Prebuilt **web** target of `sims/energy-sims` for Atomic Adventures (Clearwater Station / Diversion).

Rebuild from `game/`:

```sh
npm run sync:energy-sim
```

Requires Rust (`wasm32-unknown-unknown`) and [`wasm-pack`](https://rustwasm.github.io/wasm-pack/).
